# Mirror deploy keys (never commit)
vcr_mirror_deploy_key
vcr_mirror_deploy_key.pub
*_deploy_key
*_deploy_key.pub

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Node (only if you later add tooling)
node_modules/
npm-debug.log*

# Source uploads (originals not needed to serve the site)
uploads/
