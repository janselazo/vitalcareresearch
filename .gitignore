# Static site — keeps the repo clean.

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Node (only if you later add tooling)
node_modules/
npm-debug.log*

# Source uploads (originals not needed to serve the site)
uploads/
